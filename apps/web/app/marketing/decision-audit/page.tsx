import AppShell from "@/components/layout/AppShell";
import { getDecisionAuditTrail } from "@/lib/media-buyer/decision-audit-trail";
export const dynamic = "force-dynamic";
export default async function DecisionAuditPage() {
  const audit = await getDecisionAuditTrail({ take: 200 });
  return <AppShell><div className="space-y-6 pb-10"><section><p className="text-[10px] font-semibold uppercase tracking-[0.3em] text-teal-600">Explainable AI</p><h1 className="heading-font mt-1 text-3xl font-bold text-slate-900">Decision Audit</h1><p className="mt-2 text-sm text-slate-500">ตรวจสอบย้อนหลังว่า AI ตัดสินใจอะไร เพราะเหตุใด เมื่อใด และเกี่ยวข้องกับ Campaign หรือ Content ใด</p></section><section className="grid gap-4 sm:grid-cols-3"><article className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs text-slate-500">Decision ทั้งหมด</p><p className="mt-2 text-3xl font-bold text-slate-900">{audit.totalDecisions.toLocaleString()}</p></article><article className="rounded-3xl border border-emerald-200 bg-emerald-50 p-5"><p className="text-xs text-emerald-700">ตรวจสอบย้อนหลังได้</p><p className="mt-2 text-3xl font-bold text-emerald-800">{audit.auditableDecisions.toLocaleString()}</p></article><article className={`rounded-3xl border p-5 ${audit.invalidDecisions ? "border-rose-200 bg-rose-50" : "border-slate-200 bg-white"}`}><p className="text-xs text-slate-500">ต้องแก้ไข</p><p className="mt-2 text-3xl font-bold text-slate-900">{audit.invalidDecisions.toLocaleString()}</p></article></section><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4"><h2 className="font-semibold text-slate-900">ประวัติการตัดสินใจล่าสุด</h2></div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-100 text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="px-5 py-3">เวลา</th><th className="px-5 py-3">ประเภท</th><th className="px-5 py-3">การกระทำ</th><th className="px-5 py-3">เหตุผล</th><th className="px-5 py-3">สถานะ</th></tr></thead><tbody className="divide-y divide-slate-100">{audit.items.map((item) => <tr key={item.id}><td className="whitespace-nowrap px-5 py-4 text-xs text-slate-500">{new Date(item.createdAt).toLocaleString("th-TH", { timeZone: "Asia/Bangkok" })}</td><td className="px-5 py-4 text-xs font-medium text-slate-700">{item.decisionType}</td><td className="px-5 py-4 text-xs text-slate-600">{item.action}</td><td className="max-w-xl px-5 py-4 text-xs leading-5 text-slate-600">{item.reason}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${item.auditable ? "bg-emerald-50 text-emerald-700" : "bg-rose-50 text-rose-700"}`}>{item.auditable ? "ตรวจสอบได้" : "ต้องแก้"}</span></td></tr>)}</tbody></table></div></section></div></AppShell>;
}
